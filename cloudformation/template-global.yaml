AWSTemplateFormatVersion: '2010-09-09'

########################################
# 实验描述请在这里添加
########################################
Description: Template for CloudFormation Global

Resources:
  # TrialUseOnlyRole created by Cloudlab for user lab access, please do not modify
  TrialUseOnlyRole:
    Type: AWS::IAM::Role
    DeletionPolicy: Delete
    Properties:
      RoleName: TrialUseOnly-ContentGeneratedByGenAIDoesNotRepresentViewsOfAWS
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Sid: AllowCloudlabDelegateAssumeThisRole
            Effect: Allow
            Principal:
              AWS:
                - arn:aws:iam::585008076763:root
            Action: sts:AssumeRole
      Path: /
      Policies:
        - PolicyName: TrialUseOnlyRolePolicy
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Sid: MinimalPermissionsForTrialUseOnlyRole
                ########################################
                # 实验过程中所需的最小权限请在这里添加
                ########################################
                Action: null
                Resource:

                  null
                  ########################################
                  # 实验所用资源请在这里添加
                  ########################################

Outputs:
  TrialUseOnlyRoleArn:
    Description: ARN of the TrialUseOnlyRole
    Value: !GetAtt TrialUseOnlyRole.Arn
  ########################################
  # 实验 CFn 输出请在这里添加
  ########################################